# Mes projets Angular

Ce dépôt contient plusieurs projets Angular pour pratiquer les notions importantes.

## 📂 Projets inclus
- **demo-io** : Exemple d’utilisation de `@Input()` et `@Output()`
- **angular-project** : Projet Angular de base généré avec Angular CLI

## 🚀 Lancer un projet
```bash
cd demo-io
npm install
ng serve -o
